const express = require('express');
const router = express.Router();
const userRoute = require('../controllers/controller.js');

class Routes{

  function createPost(){
    router.post('/create/newpost', (req, res) =>{
      
    });
  }
}
